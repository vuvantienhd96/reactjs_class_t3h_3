const initialState = {
    currentUser: null,
    error: null,
    loading: false,
}

const useReducer = (state = initialState, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default useReducer;